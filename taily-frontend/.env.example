# Frontend environment (safe to commit)
#
# Copy to `taily-frontend/.env.local` for local development, or set these in Vercel.

# Backend base URL (no trailing slash). Defaults to http://localhost:8080
VITE_API_BASE_URL=http://localhost:8080

